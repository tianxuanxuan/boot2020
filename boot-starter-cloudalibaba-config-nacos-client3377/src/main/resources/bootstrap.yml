server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #Nacos服务注册中心地址

      config:
        server-addr: localhost:8848 #Nacos服务配置中心地址
        file-extension: yaml #配置文件格式
        group: DEFAULT_GROUP
        #group: DEFAULT_GROUP
        namespace: 6bbc4e41-5be4-40da-9dc9-3653c32f48c3

